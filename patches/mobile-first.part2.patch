-      if (req) {
-        try { req.call(el).catch(() => {}); } catch (_) {}
-      }
+      enterFullscreen();
     } else {
@@ -2066,4 +2187,74 @@
 
+  // ---------- Plein écran (Android) + verrou paysage ----------
+  function isFullscreen() { return !!(document.fullscreenElement || document.webkitFullscreenElement); }
+  function enterFullscreen() {
+    if (!canFullscreen || isFullscreen()) return;
+    const el = document.documentElement;
+    const req = el.requestFullscreen || el.webkitRequestFullscreen;
+    try {
+      const pr = req.call(el, { navigationUI: 'hide' });
+      if (pr && pr.then) pr.then(() => {
+        try { if (screen.orientation && screen.orientation.lock) screen.orientation.lock('landscape').catch(() => {}); } catch (_) {}
+      }).catch(() => {});
+    } catch (_) {}
+  }
+  const fsBtn = document.getElementById('fsBtn');
+  if (fsBtn) {
+    fsBtn.addEventListener('click', (e) => { e.preventDefault(); e.stopPropagation(); enterFullscreen(); });
+  }
+  ['fullscreenchange', 'webkitfullscreenchange'].forEach((ev) => document.addEventListener(ev, () => {
+    document.body.classList.toggle('is-fs', isFullscreen());
+    setTimeout(fitMobileViewport, 60);
+  }));
+
+  // ---------- Portrait : invite « Tourne ton téléphone » (+ option portrait) ----------
+  // Choix : le niveau est très horizontal (88 colonnes, ennemis qui arrivent de côté).
+  // En portrait on ne verrait que ~10 tuiles devant Sousou et les pouces couvriraient
+  // le jeu : on invite donc à tourner le téléphone, en gardant une vraie mise en page
+  // portrait (jeu en haut, commandes en bas) pour qui préfère ne pas tourner.
+  let portraitOK = false;
+  try { portraitOK = sessionStorage.getItem('gdp_portrait_ok') === '1'; } catch (_) {}
+  let rotateHold = false;
+  function checkOrientation() {
+    const playingFs = running && document.body.classList.contains('mobile-fs');
+    const portrait = window.innerHeight > window.innerWidth;
+    const need = playingFs && portrait && isPhone() && !portraitOK;
+    if (need && !rotateHold) {
+      rotateHold = true;
+      document.body.classList.add('rotate-wait');
+      if (!paused) {
+        paused = true;
+        try { AudioFX.stopMusic(); } catch (_) {}
+      }
+      keys = {};
+    } else if (!need && rotateHold) {
+      rotateHold = false;
+      document.body.classList.remove('rotate-wait');
+      if (running && paused) {
+        paused = false;
+        if (!AudioFX.isMuted()) { try { AudioFX.startMusic(hurry, levelDef.music); } catch (_) {} }
+      }
+    }
+    if (!running) document.body.classList.remove('rotate-wait');
+  }
+  const rotPortraitBtn = document.getElementById('rotPortraitBtn');
+  if (rotPortraitBtn) rotPortraitBtn.addEventListener('click', (e) => {
+    e.preventDefault();
+    portraitOK = true;
+    try { sessionStorage.setItem('gdp_portrait_ok', '1'); } catch (_) {}
+    fitMobileViewport();
+    checkOrientation();
+    if (running) snapCamera();
+  });
+  const rotMenuBtn = document.getElementById('rotMenuBtn');
+  if (rotMenuBtn) rotMenuBtn.addEventListener('click', (e) => {
+    e.preventDefault();
+    rotateHold = false;
+    document.body.classList.remove('rotate-wait');
+    returnToMenu();
+  });
+
   window.addEventListener('resize', () => {
     fitMobileViewport();
+    checkOrientation();
     if (running) snapCamera();
@@ -2078,2 +2269,3 @@
       fitMobileViewport();
+      checkOrientation();
       if (running) snapCamera();
@@ -2131,27 +2323,91 @@
 
+  // ---------- Commandes tactiles « zones » (multi-touch, pouce glissant) ----------
+  // Chaque doigt est suivi (pointerId). Le pouce gauche peut glisser de ◀ à ▶ sans
+  // se lever ; à droite, un pouce posé entre TIR et SAUT appuie sur les deux.
+  function bindZone(zone, resolve, down, up) {
+    if (!zone) return;
+    const fingers = new Map(); // pointerId -> Set(btn)
+    const pressed = new Set();
+    function sync() {
+      const now = new Set();
+      fingers.forEach((set) => set.forEach((b) => now.add(b)));
+      for (const b of Array.from(pressed)) if (!now.has(b)) { pressed.delete(b); b.classList.remove('active'); up(b); }
+      for (const b of now) if (!pressed.has(b)) { pressed.add(b); b.classList.add('active'); down(b); }
+    }
+    const onDown = (e) => {
+      if (e.cancelable) e.preventDefault();
+      e.stopPropagation();
+      try { AudioFX.resume(); } catch (_) {}
+      try { zone.setPointerCapture(e.pointerId); } catch (_) {}
+      fingers.set(e.pointerId, resolve(e.clientX, e.clientY, true));
+      sync();
+    };
+    const onMove = (e) => {
+      if (!fingers.has(e.pointerId)) return;
+      if (e.cancelable) e.preventDefault();
+      fingers.set(e.pointerId, resolve(e.clientX, e.clientY, false));
+      sync();
+    };
+    const onUp = (e) => {
+      if (!fingers.has(e.pointerId)) return;
+      fingers.delete(e.pointerId);
+      sync();
+    };
+    zone.addEventListener('pointerdown', onDown);
+    zone.addEventListener('pointermove', onMove);
+    zone.addEventListener('pointerup', onUp);
+    zone.addEventListener('pointercancel', onUp);
+    zone.addEventListener('lostpointercapture', onUp);
+    zone.addEventListener('touchstart', (e) => { if (e.cancelable) e.preventDefault(); }, { passive: false });
+    zone.addEventListener('contextmenu', (e) => e.preventDefault());
+    zone._release = () => { fingers.clear(); sync(); };
+  }
+  const inRect = (r, x, y, m) => x >= r.left - m && x <= r.right + m && y >= r.top - m && y <= r.bottom + m;
+
   function bindTouchControls() {
     if (!touchUI) return;
-
-    touchUI.querySelectorAll('[data-key]').forEach((btn) => {
-      const code = btn.dataset.key;
-      bindHoldButton(
-        btn,
-        () => {
-          keys[code] = true;
-          if (code === 'Space' && running && !paused) tryJump();
-        },
-        () => { keys[code] = false; }
-      );
-    });
-
-    touchUI.querySelectorAll('[data-action="fire"]').forEach((btn) => {
-      bindHoldButton(
-        btn,
-        () => {
-          keys['KeyX'] = true;
-          if (running && !paused) tryShoot();
-        },
-        () => { keys['KeyX'] = false; }
-      );
-    });
+    const keyOf = (b) => b.dataset.key || (b.dataset.action === 'fire' ? 'KeyX' : null);
+    const press = (b) => {
+      const code = keyOf(b);
+      if (!code) return;
+      keys[code] = true;
+      if (code === 'Space' && running && !paused) { const g = player.onGround || coyoteFrames > 0; tryJump(); if (g) haptic(8); }
+      if (code === 'KeyX' && running && !paused) tryShoot();
+    };
+    const release = (b) => { const code = keyOf(b); if (code) keys[code] = false; };
+
+    // Pouce gauche : ▲/▼ si on touche ces petits boutons, sinon ◀ ou ▶ selon le côté
+    // (le pouce peut glisser de l'un à l'autre, même un peu hors du pad).
+    const pl = document.getElementById('padLeft');
+    if (pl) {
+      const bL = pl.querySelector('.dir-left'), bR = pl.querySelector('.dir-right');
+      const bU = pl.querySelector('.dir-up'), bD = pl.querySelector('.dir-down');
+      bindZone(pl, (x, y) => {
+        const out = new Set();
+        const rU = bU.getBoundingClientRect(), rD = bD.getBoundingClientRect();
+        const rL = bL.getBoundingClientRect(), rR = bR.getBoundingClientRect();
+        if (y < rL.top - 4 && inRect(rU, x, y, 6)) { out.add(bU); return out; }
+        if (y < rL.top - 4 && inRect(rD, x, y, 6)) { out.add(bD); return out; }
+        const mid = (rL.right + rR.left) / 2;
+        out.add(x < mid ? bL : bR);
+        return out;
+      }, press, release);
+    }
+    // Pouce droit : TIR / SAUT ; un pouce posé sur la jonction appuie sur les deux
+    const pr = document.getElementById('padRight');
+    if (pr) {
+      const bF = pr.querySelector('.fire-btn'), bJ = pr.querySelector('.jump-btn');
+      bindZone(pr, (x, y) => {
+        const out = new Set();
+        const rF = bF.getBoundingClientRect(), rJ = bJ.getBoundingClientRect();
+        if (inRect(rF, x, y, 8)) out.add(bF);
+        if (inRect(rJ, x, y, 8)) out.add(bJ);
+        if (!out.size) {
+          const dF = Math.hypot(x - (rF.left + rF.right) / 2, y - (rF.top + rF.bottom) / 2);
+          const dJ = Math.hypot(x - (rJ.left + rJ.right) / 2, y - (rJ.top + rJ.bottom) / 2);
+          out.add(dF < dJ ? bF : bJ);
+        }
+        return out;
+      }, press, release);
+    }
 
@@ -2183,2 +2439,19 @@
   }, { passive: false });
+  // Tap sur l'écran = reprendre après une pause auto (appel, verrouillage…)
+  canvas.addEventListener('pointerdown', () => {
+    if (running && paused && !rotateHold && TOUCH) {
+      paused = false;
+      try { AudioFX.resume(); if (!AudioFX.isMuted()) AudioFX.startMusic(hurry, levelDef.music); } catch (_) {}
+    }
+  });
+  // Double-tap zoom (iOS) : bloqué pendant la partie hors boutons de menu
+  let lastTouchEnd = 0;
+  document.addEventListener('touchend', (e) => {
+    if (!document.body.classList.contains('playing')) return;
+    const now = Date.now();
+    if (now - lastTouchEnd < 320 && e.cancelable && !(e.target.closest && e.target.closest('.overlay, #rotatePrompt'))) e.preventDefault();
+    lastTouchEnd = now;
+  }, { passive: false });
+  canvas.addEventListener('contextmenu', (e) => e.preventDefault());
+
   // Pinch-zoom / gesture iOS pendant le jeu
@@ -2306,3 +2579,7 @@
   document.addEventListener('visibilitychange', async () => {
-    if (document.hidden) return;
+    if (document.hidden) {
+      // téléphone : appel / verrouillage / changement d'appli → pause automatique
+      if (running && !paused && TOUCH) { paused = true; keys = {}; try { AudioFX.stopMusic(); } catch (_) {} }
+      return;
+    }
     await AudioFX.resume();
@@ -2654,2 +2931,3 @@
     player.anim = 0;
+    showTouchUI(true);   // fixe la vue mobile (VW/VH) avant caméra + titres
     snapCamera();
@@ -2672,5 +2950,5 @@
       if (hintEl && !localStorage.getItem('gdp_hint_v1')) {
-        const mobile = isTouchDevice;
