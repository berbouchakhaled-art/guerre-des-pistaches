diff --git a/index.html b/index.html
index 1d2503d..0208f7d 100644
--- a/index.html
+++ b/index.html
@@ -9,2 +9,8 @@
   <meta name="format-detection" content="telephone=no" />
+  <meta name="apple-mobile-web-app-title" content="Pistaches" />
+  <meta name="application-name" content="Pistaches" />
+  <meta name="theme-color" content="#0d1a0c" />
+  <link rel="manifest" href="manifest.json" />
+  <link rel="apple-touch-icon" href="assets/icons/apple-touch-icon.png" />
+  <link rel="icon" type="image/png" sizes="192x192" href="assets/icons/icon-192.png" />
   <title>La guerre des pistaches — starring Sousou</title>
@@ -913,17 +919,19 @@
     <div id="touchUI" class="touch-ui" aria-hidden="true">
-      <div class="touch-left" role="group" aria-label="Déplacement">
-        <button type="button" class="dir-up" data-key="ArrowUp" aria-label="Viser / sauter">↑ Visée</button>
-        <button type="button" class="dir-down" data-key="ArrowDown" aria-label="Se baisser">↓ Baisser</button>
-        <button type="button" class="dir-left" data-key="ArrowLeft" aria-label="Gauche">◀ Gauche</button>
-        <button type="button" class="dir-right" data-key="ArrowRight" aria-label="Droite">Droite ▶</button>
+      <!-- Pouce gauche : zone glissable ◀ ▶ (+ petits ▲ viser / ▼ se baisser) -->
+      <div class="touch-left" id="padLeft" role="group" aria-label="Déplacement">
+        <button type="button" class="tbtn dir-up" data-key="ArrowUp" aria-label="Viser en haut"><i class="ic s1 ic-up"></i></button>
+        <button type="button" class="tbtn dir-down" data-key="ArrowDown" aria-label="Se baisser"><i class="ic s1 ic-down"></i></button>
+        <button type="button" class="tbtn dir-left" data-key="ArrowLeft" aria-label="Gauche"><i class="ic ic-left"></i></button>
+        <button type="button" class="tbtn dir-right" data-key="ArrowRight" aria-label="Droite"><i class="ic ic-right"></i></button>
       </div>
+      <!-- Pouce droit : SAUT (grand) + TIR, armes au-dessus -->
       <div class="touch-right">
         <div class="touch-weapons">
-          <button type="button" data-weapon="0" class="selected" aria-label="Lance">🗡️</button>
-          <button type="button" data-weapon="1" aria-label="Gun">🔫</button>
-          <button type="button" data-weapon="2" aria-label="Grenade">💣</button>
+          <button type="button" class="tbtn selected" data-weapon="0" aria-label="Lance"><i class="ic s1 ic-w0" style="background-position:-240px 0"></i></button>
+          <button type="button" class="tbtn" data-weapon="1" aria-label="Gun"><i class="ic s1 ic-w1" style="background-position:-264px 0"></i></button>
+          <button type="button" class="tbtn" data-weapon="2" aria-label="Grenade"><i class="ic s1 ic-w2" style="background-position:-288px 0"></i></button>
         </div>
-        <div class="touch-actions">
-          <button type="button" class="jump-btn" data-key="Space" aria-label="Sauter">Sauter</button>
-          <button type="button" class="fire-btn" data-action="fire" aria-label="Tirer">Tirer</button>
+        <div class="touch-actions" id="padRight">
+          <button type="button" class="tbtn fire-btn" data-action="fire" aria-label="Tirer"><i class="ic ic-fire"></i><span class="lb">TIR</span></button>
+          <button type="button" class="tbtn jump-btn" data-key="Space" aria-label="Sauter"><i class="ic ic-jump"></i><span class="lb">SAUT</span></button>
         </div>
@@ -932,2 +940,3 @@
     </div>
+    <button id="fsBtn" class="fs-btn" type="button" aria-label="Plein écran" title="Plein écran">Plein écran</button>
 
@@ -1006,2 +1015,7 @@
           </p>
+          <div class="touch-help" aria-label="Commandes tactiles">
+            <div><i class="ic ic-left"></i><span><b>Pouce gauche</b> : glisse sur ◀ ▶ pour courir</span></div>
+            <div><i class="ic ic-jump"></i><span><b>Pouce droit</b> : SAUT · TIR · touche une arme pour changer</span></div>
+          </div>
+          <p class="rotate-tip">📱↻ Tourne en <b>paysage</b> pour jouer en grand !</p>
         </div>
@@ -1035,2 +1049,11 @@
 
+  <div id="rotatePrompt" role="dialog" aria-modal="true" aria-labelledby="rotTitle">
+    <div class="rot-img" aria-hidden="true"></div>
+    <h2 id="rotTitle">Tourne ton téléphone !</h2>
+    <p>Sousou court bien mieux <strong>en paysage</strong> : tu vois plus loin et tes deux pouces ont de la place.</p>
+    <p class="rot-sub">Rien ne se passe ? Désactive le verrouillage de la rotation.</p>
+    <button type="button" id="rotPortraitBtn">Jouer en portrait quand même</button>
+    <button type="button" id="rotMenuBtn" class="rot-back">☰ Retour au menu</button>
+  </div>
+
   <p class="controls">
@@ -1056,2 +1079,4 @@
 
+  <link rel="stylesheet" href="css/mobile.css" />
+
   <script src="js/gfx.js"></script>
@@ -1523,4 +1548,7 @@
   ctx.imageSmoothingEnabled = false;
-  const VW = canvas.width;
-  const VH = canvas.height;
+  // Taille de vue logique : 960x540 sur ordinateur ; sur téléphone elle est
+  // recalculée (applyView) pour un zoom entier « pixel-perfect » (Sousou ≈ 15 % de l'écran).
+  let VW = canvas.width;
+  let VH = canvas.height;
+  const VIEW = window.VIEW = { mobile: false, k: 1, cssScale: 1, portrait: false, hudL: 118, hudR: 110, hudT: 10 };
   const T = 40; // taille tuile
@@ -1868,10 +1896,13 @@
     if (!sousouBgReady || !sousouBgImg.naturalWidth) return;
-    // ~70% of canvas height, slightly left of center so play stays readable
-    const targetH = VH * (lowFxMode ? 0.62 : 0.72);
-    const scale = targetH / sousouBgImg.naturalHeight;
-    const dw = Math.round(sousouBgImg.naturalWidth * scale);
-    const dh = Math.round(targetH);
+    // Portrait pixel 64x64 (assets/sousou-bg.png = 64 px x9) agrandi d'un facteur ENTIER
+    // en plus-proche-voisin : net, « vrai pixel art », grand (~65-72 % de la hauteur).
+    const base = sousouBgImg.dataset.fallback ? sousouBgImg.naturalHeight : 64;
+    let s = Math.max(2, Math.round((VH * (lowFxMode ? 0.6 : 0.68)) / base));
+    while (s > 2 && base * s > VH * 0.82) s--;
+    const dw = Math.round(sousouBgImg.naturalWidth / sousouBgImg.naturalHeight * base) * s;
+    const dh = base * s;
     const dx = Math.round(VW * 0.5 - dw * 0.5 - VW * 0.04);
     const dy = Math.round(VH * 0.5 - dh * 0.5 + VH * 0.02);
-    const alpha = lowFxMode ? 0.28 : 0.38;
+    const th = levelDef && levelDef.theme;
+    const alpha = (th === 'park' ? 0.72 : th === 'cosmo' ? 0.64 : 0.62) - (lowFxMode ? 0.08 : 0);
     ctx.save();
@@ -1879,5 +1910,5 @@
     ctx.imageSmoothingEnabled = false;
-    // world-ish feel: mild parallax so it drifts with the map, still always on-screen
-    const px = dx - Math.floor((cameraX * 0.08) % 24);
-    const py = dy - Math.floor((cameraY * 0.06) % 16);
+    // légère parallaxe, alignée sur la grille de pixels du portrait
+    const px = dx - Math.floor(((cameraX * 0.08) % 24) / s) * s;
+    const py = dy - Math.floor(((cameraY * 0.06) % 16) / s) * s;
     ctx.drawImage(sousouBgImg, 0, 0, sousouBgImg.naturalWidth, sousouBgImg.naturalHeight, px, py, dw, dh);
@@ -1994,4 +2025,16 @@
 
+  const forceTouch = /[?&]touch=1/.test(location.search);
+  const TOUCH = isTouchDevice || forceTouch;
+  document.documentElement.classList.toggle('is-touch', TOUCH);
+  // Téléphone = petit côté < 600 px CSS (les tablettes jouent aussi en portrait sans invite)
+  const isPhone = () => Math.min(window.innerWidth, window.innerHeight) < 600;
+  const canFullscreen = !!(document.documentElement.requestFullscreen || document.documentElement.webkitRequestFullscreen) && !/iPhone|iPod/.test(ua);
+  document.body.classList.toggle('can-fs', canFullscreen);
+  function haptic(pattern) {
+    if (!TOUCH || !navigator.vibrate) return;
+    try { navigator.vibrate(pattern); } catch (_) {}
+  }
+
   // Banner aide iPhone
-  if (iosBanner && (isIOS || isTouchDevice)) {
+  if (iosBanner && (isIOS || isTouchDevice) && false) {
     iosBanner.classList.add('show');
@@ -2027,2 +2070,83 @@
     }
+    applyView(w, h);
+  }
+
+  // ---------- Vue mobile : zoom ENTIER en pixels écran ----------
+  // La toile a la taille de la vue logique (VW x VH) ; le navigateur l'agrandit
+  // d'un facteur entier k de pixels physiques (image-rendering: pixelated) →
+  // pixel art net, peu de pixels à dessiner (bon pour la batterie / 60 fps).
+  let safeProbe = null;
+  function safeInsets() {
+    if (!safeProbe) {
+      safeProbe = document.createElement('div');
+      safeProbe.style.cssText = 'position:fixed;left:0;top:0;width:0;height:0;visibility:hidden;pointer-events:none;' +
+        'padding:env(safe-area-inset-top) env(safe-area-inset-right) env(safe-area-inset-bottom) env(safe-area-inset-left)';
+      document.body.appendChild(safeProbe);
+    }
+    const cs = getComputedStyle(safeProbe);
+    return { t: parseFloat(cs.paddingTop) || 0, r: parseFloat(cs.paddingRight) || 0, b: parseFloat(cs.paddingBottom) || 0, l: parseFloat(cs.paddingLeft) || 0 };
+  }
+  function setViewSize(w, h) {
+    const oldW = VW;
+    if (canvas.width !== w || canvas.height !== h) {
+      canvas.width = w; canvas.height = h;
+    }
+    VW = w; VH = h;
+    ctx.imageSmoothingEnabled = false;
+    if (typeof skyGradCache !== 'undefined') skyGradCache = { theme: null, grad: null };
+    // recentre les titres écran déjà lancés
+    if (oldW !== w && typeof nameFX !== 'undefined') {
+      for (const n of nameFX) if (n.style === 'title' || n.style === 'star') n.x += (w - oldW) / 2;
+    }
+  }
+  function applyView(cssW, cssH) {
+    const root = document.documentElement.style;
+    const fs = document.body.classList.contains('mobile-fs');
+    if (!fs) {
+      VIEW.mobile = false; VIEW.portrait = false;
+      document.body.classList.remove('portrait-play');
+      setViewSize(960, 540);
+      VIEW.hudL = 118; VIEW.hudR = 110; VIEW.hudT = 10;
+      return;
+    }
+    const dpr = Math.max(1, Math.min(window.devicePixelRatio || 1, 4));
+    const ins = safeInsets();
+    const portrait = cssH > cssW;
+    const portraitPlay = portrait && (!isPhone() || portraitOK);
+    document.body.classList.toggle('portrait-play', portraitPlay);
+    let areaX = 0, areaY = 0, areaW = cssW, areaH = cssH;
+    const TOPBAR = 56;
+    if (portraitPlay) {
+      // barre du haut (☰ armes 🔊) · jeu · panneau de commandes (reste de l'écran)
+      areaY = ins.t + TOPBAR;
+      areaH = cssH - areaY - (240 + ins.b);
+    }
+    const devW = areaW * dpr, devH = areaH * dpr;
+    let k;
+    if (portraitPlay) {
+      k = Math.max(1, Math.round(devW / (isPhone() ? 420 : 560)));
+    } else {
+      const target = cssH >= 600 ? 520 : 400; // tablette : un peu plus de champ
+      k = Math.max(1, Math.round(devH / target));
+    }
+    let vw = Math.ceil(devW / k), vh = Math.ceil(devH / k);
+    vh = Math.min(vh, 17 * T);             // jamais plus haut que le niveau (17 rangées)
+    vw = Math.min(vw, 1280);
+    const cs = k / dpr;                    // px CSS par pixel logique
+    const cw = vw * cs, ch = vh * cs;
+    const cx = Math.round(areaX + (areaW - cw) / 2);
+    const cy = Math.round(portraitPlay ? areaY : areaY + Math.max(0, (areaH - ch) / 2));
+    if (portraitPlay) root.setProperty('--panel-h', Math.max(200, Math.floor(cssH - cy - ch)) + 'px');
+    root.setProperty('--cv-w', cw + 'px');
+    root.setProperty('--cv-h', ch + 'px');
+    root.setProperty('--cv-x', cx + 'px');
+    root.setProperty('--cv-y', cy + 'px');
+    setViewSize(vw, vh);
+    VIEW.mobile = true; VIEW.portrait = portraitPlay; VIEW.k = k; VIEW.cssScale = cs;
+    if (portraitPlay) { VIEW.hudL = 6; VIEW.hudR = 6; VIEW.hudT = 6; return; }
+    const offX = cx, offY = cy;
+    // HUD entre le bouton ☰ (gauche) et 🔊 / plein écran (droite), sous l'encoche
+    VIEW.hudL = Math.ceil((Math.max(8, ins.l) + 2 + 44 + 8 - offX) / cs);
+    VIEW.hudR = Math.ceil((Math.max(8, ins.r) + 2 + 44 + (document.body.classList.contains('can-fs') && !isFullscreen() ? 52 : 0) + 8 + 3 * 44 + 12 + 8 + offX) / cs);
+    VIEW.hudT = Math.max(4, Math.ceil((Math.max(6, ins.t) + 8 - offY) / cs));
   }
@@ -2031,5 +2155,6 @@
     if (!touchUI) return;
+    if (!on) ['padLeft', 'padRight'].forEach((id) => { const z = document.getElementById(id); if (z && z._release) z._release(); });
     // iPhone / tactile / petit écran → pads visibles dès que la partie tourne
-    const force = !!on && (isTouchDevice || isIOS || window.innerWidth <= 920);
-    const useFs = !!on && (isTouchDevice || isIOS || window.innerWidth <= 920);
+    const force = !!on && (TOUCH || isIOS || window.innerWidth <= 920);
+    const useFs = !!on && (TOUCH || isIOS || window.innerWidth <= 920);
     touchUI.classList.toggle('active', force);
@@ -2040,5 +2165,5 @@
     fitMobileViewport();
+    checkOrientation();
     if (iosBanner) {
-      if (on) iosBanner.classList.remove('show');
-      else if (isIOS || isTouchDevice) iosBanner.classList.add('show');
+      iosBanner.classList.remove('show');
     }
@@ -2046,7 +2171,3 @@
     if (useFs) {
-      const el = document.documentElement;
-      const req = el.requestFullscreen || el.webkitRequestFullscreen || el.webkitRequestFullScreen;
